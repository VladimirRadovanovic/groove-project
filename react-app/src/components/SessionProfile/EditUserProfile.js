import { ProfileUpdateModal } from '../../context/Modal';

function EditUserProfile({ user, onClose }) {

    return (
        <ProfileUpdateModal onClose={onClose} >
            <div>
                djaiodoashdasdhaodhoasd
            </div>

        </ProfileUpdateModal>
    )
}

export default EditUserProfile;
